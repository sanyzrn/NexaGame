M4 approved. Build M5: Title screen, tutorial, Result screen, Hero Card and sharing. These screens decide the first impression and whether players share the game, so they must feel premium, animated and emotional, not like plain menus. Continue without stopping for phone tests.


## 1. Title screen
- Animated scene: the arena at dusk in the background (slow parallax, drifting embers, flickering braziers, waving banners), the hero on the platform in idle, and the White Div silhouette glowing faintly behind the wall.
- Game name «درفش» with the subtitle «نبرد پهلوانان», rendered with fonts (no baked text), with a gentle gold shine sweep.
- A big «نبرد!» button with an idle pulse; on tap, the camera flies down into the arena and the game starts (smooth transition, no hard cut).
- Small secondary buttons: sound toggle, and a group chip showing «لشکر دوستان» with member avatars.


## 2. Tutorial (first play only)
- Hands-on, not text-heavy: an animated ghost finger demonstrates drag → hold → release, and the game waits for the player to do it.
- Step 2: highlights the golden window the first time it appears ("now!").
- Step 3: one shield-bearer appears and a hint shows the ricochet path.
- Skippable; stored in localStorage (try/catch guarded).


## 3. Result screen
- Victory: light floods the scene, then a panel slides up. Defeat: a dignified dusk version with encouraging text, never humiliating.
- Stats counting up one by one with a tick sound: score, best combo, crits, golden-window accuracy %, damage to the group Div.
- 1–3 stars based on performance, each landing with a stamp animation and a sparkle.
- Group moment: the group Div bar appears and the player's damage drains it visibly; show «سهم تو از پیروزی لشکر: X٪».
- Buttons: «دوباره» (play again), «کارت افتخار» (Hero Card), and a «دعوت هم‌رزم» (invite) button that opens the Telegram share with text and a link (preview of the future invite feature).


## 4. Hero Card
- Rendered on card_bg in 1080x1920: hero name (Telegram first name or «پهلوان»), best stats, stars, group name and banner, the hero sprite, and a short epic line chosen by performance (e.g. «کمانش چون آرش، دلش چون رستم»; write 6–8 variations).
- Share as agreed: the Telegram share sheet with text + link, the card shown as a long-pressable image inside Telegram, and PNG download in normal browsers. Keep the prepareShare() hook.


## 5. Surprise
Add at least ONE surprising, delightful moment to these screens that makes players smile or want to share (your own idea; for example, a hidden reaction when the player gets a perfect run). Flag it in feel.ts and explain it in your summary.


All transitions animated, all text Persian/RTL with Persian digits. Commit as M5 with a short summary.